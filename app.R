# --------------------------------------------------------------
library(shiny)
# --------------------------------------------------------------
# for running in a Docker container
setwd("/home/Funnotate/")
runApp(host = "0.0.0.0", port = 8181, launch.browser = TRUE)
# --------------------------------------------------------------

