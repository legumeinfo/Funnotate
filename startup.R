# --------------------------------------------------------------
library(shiny)
# --------------------------------------------------------------

shinyApp(ui = ui, server = server, uiPattern = ".*", options = list(launch.browser = TRUE, port = 3838))

# --------------------------------------------------------------

